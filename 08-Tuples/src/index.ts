/**
 * tuples
 */

let person: [string, number, boolean] = ["anas", 21, true];

/**
 * tuples examples
 */

let hsla: [number, string, string, number];
hsla = [21, "anas", "ali", 21];

/**
 * named tuples
 */
let user: [name: string, age: number];
user = ["anas", 21];
